const DateFormat = {
  YEAR: 'YYYY',
  FULL: 'DD MMMM YYYY',
  WITH_TIME: 'YYYY/MM/DD HH:mm'
};

const DurationFormat = {
  FULL: 'H[h] m[m]',
  MINUTES: 'm[m]'
};

export { DateFormat, DurationFormat };
